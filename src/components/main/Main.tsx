import { Content, Heading } from ".";

const Main = () => {
	return (
		<main className="px-6 py-8 max-xl:px-3 bg-custom-white dark:bg-custom-black">
			<Heading />
			<Content />
		</main>
	);
};

export default Main;
