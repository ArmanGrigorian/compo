import { HeaderTop, Nav } from ".";


const Header = () => {
	return (
		<header className="bg-custom-white dark:bg-custom-black">
			<HeaderTop />
			<Nav />
		</header>
	);
};

export default Header;
